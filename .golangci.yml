version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosimple
    - gofmt
    - misspell

  settings:
    errcheck:
      check-type-assertions: true
      check-blank: true

issues:
  max-same-issues: 50
